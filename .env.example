# URL del gestionale Bombay (fonte di verità del menu).
# Il sito legge GET <URL>/api/public/menu server-side con cache 5 minuti;
# se vuoto usa il default https://www.bombayfryandgrill.it.
# Se il gestionale è irraggiungibile il sito ricade sul menu hardcoded
# di src/lib/data.ts (non si rompe mai).
NEXT_PUBLIC_GESTIONALE_URL=
